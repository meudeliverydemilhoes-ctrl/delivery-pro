{
  "name": "SOP",
  "type": "object",
  "properties": {
    "titulo": {
      "type": "string"
    },
    "descricao": {
      "type": "string"
    },
    "pilar": {
      "type": "string",
      "enum": [
        "processos",
        "desempenho",
        "tempo_potencia",
        "norte_estrategico",
        "presenca_magnetica",
        "geral"
      ]
    },
    "categoria": {
      "type": "string",
      "enum": [
        "atendimento",
        "producao",
        "gestao",
        "financeiro",
        "marketing",
        "equipe",
        "outro"
      ],
      "default": "outro"
    },
    "conteudo": {
      "type": "string",
      "description": "Conte\u00fado em texto/markdown"
    },
    "video_url": {
      "type": "string"
    },
    "imagens": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "checklist_modelo_id": {
      "type": "string",
      "description": "Checklist vinculado a este SOP"
    },
    "versao": {
      "type": "string",
      "default": "1.0"
    },
    "ativo": {
      "type": "boolean",
      "default": true
    }
  },
  "required": [
    "titulo",
    "pilar"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "data.email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "data.email": "{{user.email}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "data.email": "{{user.email}}"
    }
  }
}